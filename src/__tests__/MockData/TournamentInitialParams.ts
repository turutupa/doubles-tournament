it('Just adding this here so it doesnt prompt an error', () => {});

const name = 'Padeliers';
const date = new Date();
const location = 'La MasO';
const price = 15;
const maxNumberOfPlayers = 35;

export const tournamentInfo = {
  name,
  price,
  date,
  maxNumberOfPlayers,
  location,
};
