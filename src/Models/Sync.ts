import { Syncable } from '@interfaces/sync';

export default class Sync {
  public static import(): Syncable {
    return {} as Syncable;
  }

  public static export(): Syncable {
    return {} as Syncable;
  }
}
